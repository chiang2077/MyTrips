# 網頁修改指引 v2（沖繩・和歌山）

執行對象：`2026okinawa/index.html`、`2026wakayama/index.html`
本指引每一項都提供「找到」與「替換為」的完整字串。請嚴格照做，**不要修改指引以外的任何內容**，不要改動縮排與編碼（UTF-8）。「找到」字串在檔案中都只出現一次，可直接整段搜尋取代。

執行順序：Part A（沖繩 HTML）→ Part B（沖繩 CSS）→ Part C（和歌山 HTML）→ Part D（和歌山 CSS）→ Part E（兩檔共同新增）→ Part F（驗收清單）。

---

## Part A：沖繩 `2026okinawa/index.html` — 內容修正

### A-1. Day 1 下午時間鬆綁（萬座毛 14:30→15:30、泳池 15:30→16:30）

找到：
```html
<div class="stop"><span class="time">14:30</span><span class="node"></span><h4>萬座毛</h4>
```
替換為：
```html
<div class="stop"><span class="time">15:30</span><span class="node"></span><h4>萬座毛</h4>
```

找到：
```html
<div class="stop"><span class="time">15:30</span><span class="node"></span><h4>回飯店・泳池與海灘放空</h4>
```
替換為：
```html
<div class="stop"><span class="time">16:30</span><span class="node"></span><h4>回飯店・泳池與海灘放空</h4>
```
（注意：請先執行第一個替換再執行第二個，否則「15:30」會抓錯行。）

### A-2. Day 3 返程延後到日落之後（17:30→18:00）

找到：
```html
<div class="stop"><span class="time">17:30</span><span class="node"></span><h4>回恩納度假飯店</h4>
```
替換為：
```html
<div class="stop"><span class="time">18:00</span><span class="node"></span><h4>回恩納度假飯店</h4>
```

### A-3. Day 3 新增「半日包車」行程建議

找到：
```html
<div class="stop key"><span class="time">13:00</span><span class="node"></span><h4>搭計程車前往殘波岬</h4><p>白色燈塔佇立在斷崖之上，展望台可以眺望東海。<span class="transit">恩納→殘波岬 ≈ 計程車 20 分</span></p></div>
```
替換為：
```html
<div class="stop key"><span class="time">13:00</span><span class="node"></span><h4>搭計程車前往殘波岬</h4><p>白色燈塔佇立在斷崖之上，展望台可以眺望東海。<span class="transit">恩納→殘波岬 ≈ 計程車 20 分</span></p>
          <div class="planb"><b>半日包車建議：</b>今天恩納→殘波岬→美國村→恩納共四段車程，計程車合計約 <span class="nb">¥12,000</span>。改訂 <span class="nb">4 小時</span>觀光包車（約 <span class="nb">¥4,000–5,000</span>／小時）價差不大，還不必擔心郊區叫不到車。</div>
        </div>
```

### A-4. Day 5 出發時間對齊文案（13:00→14:30、14:00→15:00），Outlet 補午餐一句

找到：
```html
<div class="stop"><span class="time">10:00</span><span class="node"></span><h4>沖繩 Outlet Mall Ashibinaa 或瀨長島</h4><p>機場旁的暢貨中心，最後掃貨的好機會。搭計程車約 <span class="nb">10 分鐘</span>。<span class="transit">計程車 ≈ 10 分</span></p></div>
```
替換為：
```html
<div class="stop"><span class="time">10:00</span><span class="node"></span><h4>沖繩 Outlet Mall Ashibinaa 或瀨長島</h4><p>機場旁的暢貨中心，最後掃貨的好機會。搭計程車約 <span class="nb">10 分鐘</span>，午餐就在 Outlet 美食街解決。<span class="transit">計程車 ≈ 10 分</span></p></div>
```

找到：
```html
<div class="stop"><span class="time">13:00</span><span class="node"></span><h4>前往那霸機場</h4>
```
替換為：
```html
<div class="stop"><span class="time">14:30</span><span class="node"></span><h4>前往那霸機場</h4>
```

找到：
```html
<div class="stop"><span class="time">14:00</span><span class="node"></span><h4>機場最後補貨</h4>
```
替換為：
```html
<div class="stop"><span class="time">15:00</span><span class="node"></span><h4>機場最後補貨</h4>
```

### A-5. 驚安殿堂改為 Day 4 白天順路（動線修正）

找到：
```html
<div class="shop"><span class="when opt">DAY 4 夜</span><div><h4>驚安殿堂 國際通店</h4><p><span class="nb">24 小時</span>營業，零食藥妝最便宜，晚上回飯店前掃貨。</p>
```
替換為：
```html
<div class="shop"><span class="when">DAY 4</span><div><h4>驚安殿堂 國際通店</h4><p><span class="nb">24 小時</span>營業，零食藥妝最便宜，下午逛國際通時順路掃貨。</p>
```

### A-6. 附錄 A3 新增叫車提醒

找到：
```html
        <p>那霸市內單軌電車「Yui-Rail」可到達國際通、牧志市場、首里城、波上宮、新都心，基本上市區觀光不需要車。</p>
      </div></details>
```
替換為：
```html
        <p>那霸市內單軌電車「Yui-Rail」可到達國際通、牧志市場、首里城、波上宮、新都心，基本上市區觀光不需要車。</p>
        <p class="warn"><b>叫車提醒：</b>恩納屬郊區，傍晚熱門時段不易臨時叫到計程車，叫車 App 涵蓋也不穩定。回程請提前請店家或飯店櫃檯代叫，或事先預約。</p>
      </div></details>
```

### A-7. Day 3 emoji 換掉（🎡 → 🏖️）

找到：
```html
<div class="big">03<span class="emo">🎡</span></div>
```
替換為：
```html
<div class="big">03<span class="emo">🏖&#xFE0F;</span></div>
```

### A-8. Day 5 emoji 補彩色變體符號

找到：
```html
<div class="big">05<span class="emo">🛍</span></div>
```
替換為：
```html
<div class="big">05<span class="emo">🛍&#xFE0F;</span></div>
```

### A-9. 修正錯誤註解

找到：
```html
    <!-- Scene: Naha cityscape -->
```
替換為：
```html
    <!-- Scene: Cape Zanpa lighthouse -->
```

### A-10. 場景圖說改成圖內覆蓋式（HTML 部分，共 5 處）

以下 5 處，把 `<p class="scap">…</p>` 改為 `<span class="cap">…</span>`（標籤與 class 都要改，文字不變）：

找到：
```html
      <p class="scap">恩納西海岸黃昏夕陽海景</p>
```
替換為：
```html
      <span class="cap">恩納西海岸黃昏夕陽海景</span>
```

找到：
```html
      <p class="scap">美麗海水族館黑潮之海鯨鯊</p>
```
替換為：
```html
      <span class="cap">美麗海水族館黑潮之海鯨鯊</span>
```

找到：
```html
      <p class="scap">殘波岬白色燈塔海岸</p>
```
替換為：
```html
      <span class="cap">殘波岬白色燈塔海岸</span>
```

找到：
```html
      <p class="scap">首里城黃昏景色</p>
```
替換為：
```html
      <span class="cap">首里城黃昏景色</span>
```

找到：
```html
      <p class="scap">那霸機場飛機起飛藍天白雲</p>
```
替換為：
```html
      <span class="cap">那霸機場飛機起飛藍天白雲</span>
```

---

## Part B：沖繩 `2026okinawa/index.html` — CSS 修正

### B-1. 場景圖規格重寫（拿掉 opacity:.88、加圖內漸層與圖說）

找到：
```css
  .scene{width:100%;max-width:680px;margin:0 auto 20px;display:block;opacity:.88;}
  .scene img{width:100%;height:280px;object-fit:cover;display:block;border-radius:16px;filter:drop-shadow(0 4px 20px rgba(4,28,48,.3));}
```
替換為：
```css
  .scene{position:relative;width:100%;max-width:680px;margin:0 auto 20px;overflow:hidden;border-radius:16px;box-shadow:0 6px 24px rgba(4,28,48,.3);}
  .scene img{width:100%;height:280px;object-fit:cover;display:block;}
  .scene::after{content:"";position:absolute;inset:0;background:linear-gradient(transparent 55%,rgba(0,0,0,.55));pointer-events:none;z-index:1;}
  .scene .cap{position:absolute;left:16px;bottom:12px;z-index:2;color:#fff;font-size:.95rem;font-weight:700;text-shadow:0 2px 8px rgba(0,0,0,.7);line-height:1.5;}
```

### B-2. 刪除舊圖說樣式

找到並**整行刪除**：
```css
  .scene .scap{margin:6px 4px 0;font-size:.9rem;color:rgba(255,255,255,.85);text-align:right;text-shadow:0 1px 6px rgba(4,28,48,.5);}
```

### B-3. Ken Burns 動畫改為進入視窗才播放

找到：
```css
  @media(prefers-reduced-motion:no-preference){
    .scene img{animation:kb 9s ease-out both;}
    @keyframes kb{from{transform:scale(1.06);}to{transform:scale(1);}}
  }
```
替換為：
```css
  @media(prefers-reduced-motion:no-preference){
    .reveal.in .scene img,.scene.reveal.in img{animation:kb 9s ease-out both;}
    @keyframes kb{from{transform:scale(1.06);}to{transform:scale(1);}}
  }
```

### B-4. 桌機右側導覽列改恆定深色底（淺色天空段不再隱形）

找到：
```css
  .rail{position:fixed;right:10px;top:50%;transform:translateY(-50%);z-index:50;display:flex;flex-direction:column;gap:10px;}
  .rail a{display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:50%;
    background:rgba(255,255,255,.3);border:1px solid rgba(255,255,255,.45);
    -webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px);
    font-family:var(--mono);font-size:0.98rem;font-weight:700;color:rgba(255,255,255,.8);text-decoration:none;
    transition:all .35s;box-shadow:0 2px 8px rgba(4,28,48,.2);}
```
替換為：
```css
  .rail{position:fixed;right:10px;top:50%;transform:translateY(-50%);z-index:50;display:flex;flex-direction:column;gap:8px;
    padding:8px 5px;border-radius:999px;background:rgba(10,20,30,.5);
    -webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px);}
  .rail a{display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:50%;
    background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.25);
    font-family:var(--mono);font-size:0.98rem;font-weight:700;color:#fff;text-decoration:none;
    transition:all .35s;box-shadow:0 2px 8px rgba(4,28,48,.2);}
```

### B-5. 每日浮水印大數字提高對比

找到：
```css
  .dhead .big{font-family:var(--mono);font-weight:800;font-size:clamp(4.8rem,22vw,8.5rem);line-height:.88;color:rgba(255,255,255,.18);letter-spacing:-.04em;text-shadow:0 2px 24px rgba(4,28,48,.15);user-select:none;}
```
替換為：
```css
  .dhead .big{font-family:var(--mono);font-weight:800;font-size:clamp(4.8rem,22vw,8.5rem);line-height:.88;color:rgba(255,255,255,.3);letter-spacing:-.04em;text-shadow:0 2px 24px rgba(4,28,48,.35);user-select:none;}
```

---

## Part C：和歌山 `2026wakayama/index.html` — 內容修正

### C-1. 場景卡改成真正的 `<img>`（共 6 處）

以下 6 處逐一替換（背景圖 div 改為 img 標籤，圖說文字不變）：

找到：
```html
<div class="scene" style="background:url('images/day1.jpg') center/cover no-repeat"><span class="cap">關西機場黃昏降落</span></div>
```
替換為：
```html
<div class="scene"><img src="images/day1.jpg" alt="關西機場黃昏降落" loading="lazy" decoding="async"><span class="cap">關西機場黃昏降落</span></div>
```

找到：
```html
<div class="scene" style="background:url('images/day2.jpg') center/cover no-repeat"><span class="cap">白濱円月島夕陽</span></div>
```
替換為：
```html
<div class="scene"><img src="images/day2.jpg" alt="白濱円月島夕陽" loading="lazy" decoding="async"><span class="cap">白濱円月島夕陽</span></div>
```

找到：
```html
<div class="scene" style="background:url('images/day3.jpg') center/cover no-repeat"><span class="cap">那智瀑布秋楓紅葉</span></div>
```
替換為：
```html
<div class="scene"><img src="images/day3.jpg" alt="那智瀑布秋楓紅葉" loading="lazy" decoding="async"><span class="cap">那智瀑布秋楓紅葉</span></div>
```

找到：
```html
<div class="scene" style="background:url('images/day4.jpg') center/cover no-repeat"><span class="cap">三段壁斷崖海浪</span></div>
```
替換為：
```html
<div class="scene"><img src="images/day4.jpg" alt="三段壁斷崖海浪" loading="lazy" decoding="async"><span class="cap">三段壁斷崖海浪</span></div>
```

找到：
```html
<div class="scene" style="background:url('images/day5.jpg') center/cover no-repeat"><span class="cap">和歌山城秋楓</span></div>
```
替換為：
```html
<div class="scene"><img src="images/day5.jpg" alt="和歌山城秋楓" loading="lazy" decoding="async"><span class="cap">和歌山城秋楓</span></div>
```

找到：
```html
<div class="scene" style="background:url('images/day6.jpg') center/cover no-repeat"><span class="cap">臨空城 Outlet</span></div>
```
替換為：
```html
<div class="scene"><img src="images/day6.jpg" alt="臨空城 Outlet" loading="lazy" decoding="async"><span class="cap">臨空城 Outlet</span></div>
```

### C-2. 清除 Day 2 兩個殘留空行

找到（`</p>` 之後有一行空白縮排）：
```html
        <div class="stop key"><span class="time">11:45</span><span class="node"></span><h4>黑潮號特急列車南下</h4><p>指定席約 <span class="nb">¥5,000／人</span>。右側靠海座位風景最好，沿線是太平洋與蜜柑山。<span class="drive">日根野 → 白濱 ≈ 1h40m</span></p>
          
        </div>
```
替換為：
```html
        <div class="stop key"><span class="time">11:45</span><span class="node"></span><h4>黑潮號特急列車南下</h4><p>指定席約 <span class="nb">¥5,000／人</span>。右側靠海座位風景最好，沿線是太平洋與蜜柑山。<span class="drive">日根野 → 白濱 ≈ 1h40m</span></p></div>
```

找到：
```html
        <div class="stop key"><span class="time">14:00</span><span class="node"></span><h4>川久旅館・寄行李逛大廳</h4><p>15:00 前先寄行李。金色穹頂、羅馬柱、名家美術品——「可以住的美術館」大廳本身就是景點。</p>
          
        </div>
```
替換為：
```html
        <div class="stop key"><span class="time">14:00</span><span class="node"></span><h4>川久旅館・寄行李逛大廳</h4><p>15:00 前先寄行李。金色穹頂、羅馬柱、名家美術品——「可以住的美術館」大廳本身就是景點。</p></div>
```

### C-3. Day 6 emoji 補彩色變體符號

找到：
```html
<div class="big">06<span class="emo">🛍</span></div>
```
替換為：
```html
<div class="big">06<span class="emo">🛍&#xFE0F;</span></div>
```

### C-4. 範圍符號統一為「–」（共 3 處）

找到：
```html
<li><b>全室套房</b>　70~90m² 起，和洋室、按摩浴缸等多種房型</li>
```
替換為：
```html
<li><b>全室套房</b>　70–90m² 起，和洋室、按摩浴缸等多種房型</li>
```

找到：
```html
<span class="px"><span class="nb">¥26,500</span>~80,000/人/晚</span>
```
替換為：
```html
<span class="px"><span class="nb">¥26,500–80,000</span>/人/晚</span>
```

找到：
```html
<tr><td>時間</td><td>慢走含參拜約 2~<span class="nb">2.5 小時</span></td></tr>
```
替換為：
```html
<tr><td>時間</td><td>慢走含參拜約 <span class="nb">2–2.5 小時</span></td></tr>
```

---

## Part D：和歌山 `2026wakayama/index.html` — CSS 修正

### D-1. 移除 18px 基準字級（與沖繩版統一開本）

找到：
```css
  html{scroll-behavior:smooth;font-size:18px;}
```
替換為：
```css
  html{scroll-behavior:smooth;}
```

找到並**整段刪除**（含前後空行擇一保留即可）：
```css
  @media(max-width:600px){
    html{font-size:16px;}
  }
```

### D-2. 場景卡 CSS 改為 img 版本

找到：
```css
  .scene{position:relative;overflow:hidden;border-radius:16px;
    min-height:220px;margin:10px 0 6px;
    display:flex;align-items:flex-end;padding:16px 18px;
    box-shadow:0 6px 22px rgba(40,12,4,.3);
  }
  .scene::after{
    content:"";position:absolute;inset:0;
    background:linear-gradient(transparent 25%,rgba(0,0,0,.55));z-index:1;
    pointer-events:none;
  }
  .scene .cap{
    color:#fff;font-size:0.95rem;font-weight:700;
    text-shadow:0 2px 8px rgba(0,0,0,.7);z-index:2;line-height:1.5;
  }
```
替換為：
```css
  .scene{position:relative;overflow:hidden;border-radius:16px;margin:10px 0 6px;box-shadow:0 6px 22px rgba(40,12,4,.3);}
  .scene img{width:100%;height:240px;object-fit:cover;display:block;}
  .scene::after{content:"";position:absolute;inset:0;background:linear-gradient(transparent 55%,rgba(0,0,0,.55));pointer-events:none;z-index:1;}
  .scene .cap{position:absolute;left:16px;bottom:12px;z-index:2;color:#fff;font-size:0.95rem;font-weight:700;text-shadow:0 2px 8px rgba(0,0,0,.7);line-height:1.5;}
```

### D-3. 桌機右側導覽列改恆定深色底

找到：
```css
  .rail{position:fixed;right:10px;top:50%;transform:translateY(-50%);z-index:50;display:flex;flex-direction:column;gap:10px;}
  .rail a{display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:50%;
    background:rgba(255,255,255,.35);border:1px solid rgba(255,255,255,.5);
    -webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px);
    font-family:var(--mono);font-size:0.98rem;font-weight:700;color:rgba(61,28,11,.75);text-decoration:none;
    transition:all .3s;box-shadow:0 2px 8px rgba(40,12,4,.15);}
```
替換為：
```css
  .rail{position:fixed;right:10px;top:50%;transform:translateY(-50%);z-index:50;display:flex;flex-direction:column;gap:8px;
    padding:8px 5px;border-radius:999px;background:rgba(10,20,30,.5);
    -webkit-backdrop-filter:blur(10px);backdrop-filter:blur(10px);}
  .rail a{display:flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:50%;
    background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.25);
    font-family:var(--mono);font-size:0.98rem;font-weight:700;color:#fff;text-decoration:none;
    transition:all .3s;box-shadow:0 2px 8px rgba(40,12,4,.15);}
```

### D-4. 每日浮水印大數字提高對比

找到：
```css
  .dhead .big{font-family:var(--mono);font-weight:800;font-size:clamp(4.2rem,18vw,7.5rem);line-height:.9;color:rgba(255,255,255,.22);letter-spacing:-.04em;text-shadow:0 2px 20px rgba(40,12,4,.15);user-select:none;}
```
替換為：
```css
  .dhead .big{font-family:var(--mono);font-weight:800;font-size:clamp(4.2rem,18vw,7.5rem);line-height:.9;color:rgba(255,255,255,.3);letter-spacing:-.04em;text-shadow:0 2px 20px rgba(40,12,4,.35);user-select:none;}
```

### D-5. 新增 Ken Burns 動畫（和歌山原本沒有）

找到：
```css
  #pbar{position:fixed;top:0;left:0;height:3px;width:0;background:var(--accent);z-index:99;}
```
替換為：
```css
  @media(prefers-reduced-motion:no-preference){
    .reveal.in .scene img,.scene.reveal.in img{animation:kb 9s ease-out both;}
    @keyframes kb{from{transform:scale(1.06);}to{transform:scale(1);}}
  }

  #pbar{position:fixed;top:0;left:0;height:3px;width:0;background:var(--accent);z-index:99;}
```

---

## Part E：兩檔共同新增 — iPhone 15 Pro（393px）閱讀寬度調校

**兩個檔案都要做。**在 `</style>` 標籤的**正前方**（即 `#pbar{...}` 那行之後）插入以下區塊：

沖繩 `2026okinawa/index.html` 插入：
```css
  /* iPhone 15 Pro（393px）窄幅調校 */
  @media(max-width:430px){
    .wrap{padding:0 14px;}
    .day{padding:16px 14px 8px;}
    .stop{padding-left:58px;}
    .stop::before{left:47px;}
    .stop .node{left:41.5px;}
    .stop .time{width:38px;font-size:.78rem;}
    .scene img{height:200px;}
    .hero .glass{padding:26px 20px 24px;}
  }
```

和歌山 `2026wakayama/index.html` 插入（scene 高度不同，其餘相同）：
```css
  /* iPhone 15 Pro（393px）窄幅調校 */
  @media(max-width:430px){
    .wrap{padding:0 14px;}
    .day{padding:16px 14px 8px;}
    .stop{padding-left:58px;}
    .stop::before{left:47px;}
    .stop .node{left:41.5px;}
    .stop .time{width:38px;font-size:.78rem;}
    .scene img{height:190px;}
    .hero .glass{padding:26px 20px 24px;}
  }
```

說明：393px 下時間軸縮排從 64px 收到 58px、時間欄 38px，內文欄可多容納約 1 個中文字；此區塊放在檔案最末，會覆蓋前面 520px 斷點的同名屬性，這是刻意的，不要合併進舊斷點。

---

## Part F：驗收清單（全部做完後逐項確認）

1. 沖繩 Day 1 時間軸依序為 11:00 / 11:45 / 14:00 / **15:30 萬座毛** / **16:30 泳池** / 17:40 / 18:45。
2. 沖繩 Day 3 的 13:00 殘波岬 stop 內出現黃色「半日包車建議」框；返程時間是 **18:00**。
3. 沖繩 Day 5 依序為 09:00 / 10:00 / **14:30 前往機場** / **15:00 補貨** / 17:30。
4. 沖繩購物區「驚安殿堂」標籤為藍綠色 **DAY 4**（不再是灰色 DAY 4 夜），文字為「下午逛國際通時順路掃貨」。
5. 沖繩附錄 A3 最後出現橘色「叫車提醒」段落。
6. 沖繩 Day 3 大數字旁 emoji 是 🏖️，Day 5 與和歌山 Day 6 的 🛍️ 為彩色。
7. 兩頁的場景圖說都疊在**圖片內左下角**（白字＋底部漸層），沖繩圖片不再有整體變暗的濾鏡。
8. 和歌山 6 張場景圖在 HTML 中都是 `<img>` 標籤且帶 `loading="lazy"`。
9. 兩頁桌機寬度下，右側圓點導覽列有一條深色膠囊底，在任何天空顏色下都清晰可見。
10. 和歌山 `<style>` 內搜尋不到 `font-size:18px`。
11. 兩頁 `</style>` 前都有 `@media(max-width:430px)` 區塊。
12. 用瀏覽器開發工具切到 393×852（iPhone 15 Pro）檢視兩頁：時間軸文字不與時間欄重疊、底部膠囊導覽不遮住最後一段內容、無水平捲軸。
13. 搜尋兩檔確認已無 `scap`、無 `style="background:url(`、無 `~`（波浪號範圍符）。
